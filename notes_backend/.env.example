# Notes Backend Environment Variables

# Host interface to bind the server (default: 0.0.0.0)
HOST=0.0.0.0

# Port for the server (default: 3001)
PORT=3001

# Node environment (development|production|test)
NODE_ENV=development
